{
  "name": "fragments-ui",
  "private": true,
  "version": "0.0.1",
  "description": "Fragments UI testing web app",
  "scripts": {
    "start": "npx parcel build src/index.html",
    "build": "npx parcel build src/index.html",
    "serve": "parcel serve src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Vantonage/fragments-ui.git"
  },
  "author": "Ricky Chen rchen100@myseneca.ca",
  "license": "UNLICENSED",
  "devDependencies": {
    "buffer": "^5.7.1",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "aws-amplify": "^5.0.10",
    "dotenv": "^16.0.3"
  }
}
